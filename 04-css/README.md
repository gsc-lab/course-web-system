# 04 - CSS 기본 예제

CSS 선택자, 속성, 외부 스타일 시트 연결을 학습하기 위한 실습 파일이다.

## 실행

Docker 불필요. 브라우저에서 `examples/index.html` 파일을 직접 열면 된다.

## 파일 구조

```
04-css/
└── examples/
    ├── index.html   # HTML 문서 (외부 CSS 링크)
    └── style.css    # 외부 스타일 시트
```
